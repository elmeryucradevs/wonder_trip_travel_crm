import 'package:flutter/material.dart';

/// ---
/// [AppColors] define la paleta de colores centralizada para el rediseño
/// de la interfaz de usuario de "Wonder Trip Travel".
///
/// Inspirada en diseños modernos, utiliza un fondo suave y colores de acento
/// vibrantes para crear una experiencia visual elegante y profesional.
/// ---
class AppColors {
  // Paleta para el Modo Claro
  static const Color primaryLight = Color(0xFF4A43EC); // Un violeta/azul vibrante
  static const Color accentLight = Color(0xFF4DD3A1); // Un verde menta para acentos
  static const Color backgroundLight = Color(0xFFF7F7FF); // Un blanco con un toque de lavanda
  static const Color cardLight = Colors.white;
  static const Color fontTitleLight = Color(0xFF1B1B1B);
  static const Color fontBodyLight = Color(0xFF575757);
  static const Color fontSubtitleLight = Color(0xFF8A8A8A);

  // Paleta para el Modo Oscuro
  static const Color primaryDark = Color(0xFF6C63FF); // Un violeta más brillante para contraste
  static const Color accentDark = Color(0xFF50E3C2); // Un turquesa vibrante
  static const Color backgroundDark = Color(0xFF121212);
  static const Color cardDark = Color(0xFF1E1E1E);
  static const Color fontTitleDark = Color(0xFFFFFFFF);
  static const Color fontBodyDark = Color(0xFFB0B0B0);
  static const Color fontSubtitleDark = Color(0xFF7A7A7A);
}